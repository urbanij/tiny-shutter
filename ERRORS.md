* intervallometer does not work as intended. need to fix the interrupt.


* *PLUS*:
	- make the delay of *delayed shoot* function selectable by the user.
	- make the delay of *intervallometer* function selctable by the user. 
